# Backlog

## Текущие задачи

- [x] обновить readme, добавить лицензию
- [ ] при обновлениях бот подключается второй раз - проверить (возможно проблема
      только в dev режиме из-за hmr, в проде скорее всего повторяться не будет)
- [ ] команда !заказ - не работает, после выполенного заказа следующий взятый не
      отображается на мониторе
- [ ] нет уведомления о просроченном заказе
- [ ] истекший заказ не пропадает из монитора взятых заказов (или долго
      пропадает?)
- [ ] заполнять пустые входящие заказы сразу по несколько штук, если в этот тик
      были пустые слоты. нужен ли таймер? с таймером есть пауза между заказами и
      если два игрока выберут один и тот же заказ одновременно, второй
      обработанный игрок без таймера может получить тот заказ, который он не
      хотел брать и еще не видел его состав
- [ ] после выполнения заказа - он должен быть удалён у исполнителя (сейчас
      команда !заказ показывает последний заказ и пакет\поднос)
- [ ] вывести номер слота входящего заказа (1, 2, 3) сверху таблички заказа, а
      не в самом заказе, сделать монитор взятых заказов симметричным - выводить
      исполнителя сверху таблички заказа, а не в самом заказе
- [ ] генерация заказов - переработать, заказы на 1\2\3\4 человек
- [ ] придирчивать отображать в звёздочках
- [ ] переделать sse на событийный подход - уведомления вместо чата, и другие
      события - анимации заказов, анимация рецепта и т.д.
- [ ] подсказки по командам, короткие названия команд
- [ ] проверить как считается правильность заказа - лишняя котлета в бургере
      засчиталась как "идеально". может сделать лишние ингридиенты слабо
      влияющими на вердикт? надо учитывать тогда сортировку с удалением лишних.
      `котлета + котлета + сыр` - посередине котлета - лишняя для рецепта
      `котлета + сыр`

## После MVP

Задачи, отложенные за пределы MVP. В `docs/plan.md` и `docs/sim.md` — только
запланированное к реализации; всё остальное живёт здесь.

- [ ] рассмотреть физику столкновений персонажей
- [ ] очереди у станций: несколько персонажей у одной станции ждут
- [ ] рассмотреть зависимость скорости персонажа от уровня
- [ ] pathfinding по проходам кухни
- [ ] второй поднос и «оставь поднос на станции» (???)
- [ ] несколько заказов у одного игрока, очередь заказов на кухне (???)
- [ ] обдумать автозавершение текущего MenuItem (сейчас переход к следующему
      блюду — только по `!next`)
- [ ] уборка/мойка станций
- [ ] готовка блюда - жарка котлеты для бургера или испекание пиццы
- [ ] переписать на neverthrow или effect-ts - всё равно повсюду используется
      `{ ok: boolean, ...}`
- [ ] звук и эффекты оверлея по событиям SIM
- [ ] анимации мониторов - получения\удаления заказов, просмотр рецепта и т.д.
      [pixijs-actions](https://github.com/reececomo/pixijs-actions)
- [ ] пересмотреть хрупкость всех тестов: захардкоженные значения (XP за
      вердикты, тексты реплик) ломаются при любой правке баланса — проверять
      поведение, а не конкретные числа
- [ ] сортировка ингридиентов в поиске, если собирать бургер - то при "добавить
      булоч" вниз кладется нижняя булочка, а вверх - верхняя булочка
- [ ] проверка блюд - если клиент заказал пиццу и бургер, а исполнитель сделал в
      другом порядке - сперва бургер, затем пиццу - надо отсортировать по
      совпадению и выдать результат на основе максимальных совпадений

## Решить до старта MVP

- [ ] `TICK_HZ` и `SPEED`: стартовые значения (крутить на плейтесте)
- [x] тест-раннер: `vitest` или `node --test` без новых зависимостей
- [x] кто инициирует следующий заказ после `serve`: учётка (как в плане) или SIM
      — решено в O1: авто-выдачи нет, игрок берёт заказ с доски сам
      (`takeOrder`, команда `!взять` придёт в O2)
