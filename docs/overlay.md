# OBS Overlay: план

## Архитектура

- SvelteKit на `@sveltejs/adapter-node`, один Node-процесс.
- `hooks.server.ts` — корень приложения: создаёт состояние игры, стартует
  чат-бот (twurple). Гард — корень инициализируется один раз.
- `src/routes/api/overlay/sse/+server.ts` — SSE-эндпоинт, отдаёт кадры снапшота
  оверлея.
- Запуск: `pnpm dev` (полный стек), прод — `pnpm build && node build`, порт из
  `.env`. Бот и оверлей живут в одном Node-процессе (hooks).

## Игровые механики

- `!join` удаляется из парсера и replies.
- Заказы появляются во входящих (3 слота) автоматически; интервалы появления и
  время жизни невзятого заказа настраиваются в `config`.
- `!взять N` — игрок берёт заказ из слота; сессия и персонаж создаются лениво; у
  игрока один активный заказ.
- Заказ собирается по блюдам: игрок готовит текущий MenuItem, `!next`
  запечатывает снятые слои и очищает поднос для следующего блюда. `!serve`
  отдаёт весь заказ целиком (запечатанные блюда + текущее), после оценки + XP
  игрок снова свободен для `!взять`.
- Время на заказ — таймер (настраивается в `config`), оценка и XP — через
  `OrderValidator`/`scoring`.
- `!заказ` — напоминание состава своего заказа (выводится на мониторе
  исполнения, см. ниже) — отдельная команда, не связана со взятием.
- `!рецепт <блюдо>` — отобразить рецепт на окне рецептов; неизвестное блюдо —
  «нет такого блюда».
- `!put`, `!bin`, `!menu` — без изменений.
- Команды: `!взять`/`!take <1|2|3>`, `!заказ`/`!order`, `!next`,
  `!рецепт`/`!recipe <блюдо>`.
- Ответы-отказы: «слот пуст», «у тебя уже есть заказ», «нет активного заказа»,
  «это было последнее блюдо заказа», «нет такого блюда».

## Мониторы

- Входящие — монитор из 3 слотов: заказы, ожидающие взятия.
- Исполнения — монитор из 3 фиксированных слотов: карточка показывает
  «исполнитель» и состав заказа (MenuItem). По `!заказ` карточка своего заказа
  занимает слот по FIFO (перезапись старейшего); позиции слотов не меняются.
- Рецепты — отдельное окно с одним рецептом.

## Снапшот оверлея (состав)

Три карточки — три разных типа (см. `src/lib/overlay/types.ts`):

- `IncomingOrder` — входящий заказ: блюда, «придирчивость» клиента (0–5), срок
  выполнения (`deadline`, epoch ms).
- `ExecutionOrder` — карточка исполнения: исполнитель, блюда с отметкой
  готовности (запечатано/нет), срок выполнения.
- `RecipeCard` — рецепт: название + ингредиенты.

- Сцена: станции (полки с ингредиентами, мусорка, выдача, терминал рецептов) и
  персонажи (позиция, поднос, заказ).
- Заказ игрока над головой — **MenuItem**: пустой силуэт для неготового блюда,
  заполненный — для запечатанного, текущее подсвечивается.
- Доски: входящие (3 слота), исполнения (3 слота), рецепт (1 окно).

## Фронт

- `pixi.js` v8 + Svelte 5, один `<canvas>`, обвязка на Svelte.
- `src/routes/+page.svelte` — главная страница и есть оверлей: один canvas для
  всего: сцена (кухня, персонажи), мониторы (входящие, исполнения), окно
  рецепта.
- `src/lib/overlay/overlay-store.ts` — `EventSource("/api/overlay/sse")` →
  store.
- `src/lib/overlay/pixi-scene.ts`, `src/lib/overlay/pixi-boards.ts` — сцена и
  мониторы, рисунок из снапшота.
- `src/lib/overlay/loader.ts` — загрузка ассетов по манифесту.

## Ассеты

- `static/assets/manifest.json` — список ресурсов (Pixi `Assets`).
- `static/assets/ingredients/<id>.png` — иконки ингредиентов / простых
  предметов.
- `static/assets/stations/<kind>.png` — полка, мусорка, выдача, терминал.
- `static/assets/characters/base.png` — спрайт персонажа.
- `static/assets/boards/frame.png` — рамка слота монитора.
- Отсутствующий в манифесте ассет не рисуется (пустые слоты, без заглушек).
- Тень над головой — тот же спрайт с серым tint.

## Шаги

**До O0** — адаптер static → node, `vite.config.ts`, `PORT` в `.env`, каталог
ассетов + манифест.

| Шаг    | Что делаем                                                                             | DoD                                                              |
| ------ | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| **O0** | скаффолд оверлея: `<canvas>`, загрузчик ассетов, SSE-подключение                       | страница открывается, canvas живой                               |
| **O1** | входящие заказы: автоген, слоти, время жизни — + тесты                                 | тесты: наполнение до 3 слотов, сгорание, взятие пустого/занятого |
| **O2** | команды: удалить `!join`, добавить `!взять`, `!заказ`, `!next`, `!рецепт` + тесты      | `pnpm test` по сценарию «взял → собрал → serve → снова свободен» |
| **O3** | проекция: снапшот оверлея, FIFO-слоты монитора исполнения, прогресс MenuItem — + тесты | тесты: старейший слот перезаписывается, позиции стабильны        |
| **O4** | SSE-роут + корень в `hooks.server.ts`, общий SessionManager, бот                       | SSE стримит кадры; бот и оверлей в одном процессе                |
| **O5** | pixi-сцена и мониторы по снапшоту                                                      | картинка обновляется из SSE, ассеты из манифеста                 |
| **O6** | OBS: ссылки Browser Sources, `.env`, README                                            | полный стек в прод-сборке, 4 источника работают                  |

## Чеклист задач

### 1. Инфра

- [x] adapter-static → `@sveltejs/adapter-node` в `vite.config.ts`
- [x] `hooks.server.ts` — гард инициализации (один раз)
- [x] `PORT` из `.env` в dev и prod
- [x] добавить `pixi.js` v8 в dependencies

### 2. O0: скаффолд оверлея

- [x] `static/assets/manifest.json` + каталоги `ingredients/`, `stations/`,
      `characters/`, `boards/`
- [x] `src/lib/overlay/loader.ts` — загрузка ассетов по манифесту
- [x] `src/routes/+page.svelte` — один canvas, все мониторы на нём (главная
      страница)
- [x] `src/lib/overlay/overlay-store.ts` — `EventSource` → store

### 3. SSE + интеграция

- [x] SSE-роут `/api/overlay/sse` — стрим кадров снапшота (пока заглушка)
- [x] общий `SessionManager` — бот и оверлей в одном процессе

### 4. pixi-отрисовка

- [x] `src/lib/overlay/pixi-scene.ts` — кухня, персонажи, мониторы
- [x] `src/lib/overlay/pixi-boards.ts` — доски (входящие, исполнение, рецепт)

### 5. O1: входящие заказы

- [x] автоген, 3 слота, время жизни, сгорание + тесты
- [x] `takeOrder` в SM, авто-выдача удалена, `!join` удалён (взятие с доски —
      команда `!взять` в O2; между O1 и O2 у игроков нет команд)

### 6. O2: команды

- [x] удалить `!join`, добавить `!взять`/`!заказ`/`!next`/`!рецепт` + тесты
      (план и шаги — `docs/o2-commands.md`; `!join` удалён ещё в O1)

### 7. O3: снапшот оверлея

- [ ] проекция, FIFO-слоты исполнения, прогресс MenuItem + тесты

### 8. O6: OBS

- [ ] OBS Browser Sources — 4 ссылки, `.env`, README
